// Business logic required for Engagement creation and recurring generation.

const createEngagement = async (data, user) => {
  // layering remaing
};

const generateNextPeriod = async (engagementId, user) => {
  // layering remaining
};

module.exports = { createEngagement, generateNextPeriod };
